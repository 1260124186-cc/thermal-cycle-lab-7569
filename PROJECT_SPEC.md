# 热循环实验协调服务规格

## 项目目标
为离线材料实验室提供一个内存型 HTTP 服务，统一管理试样安全温区、温控曲线、试验生命周期、传感帧与阶段报告。服务不依赖外部数据库或在线设备，可用于本地演示和自动化验证。

## 用户角色
- 实验工程师：登记试样并启动、暂停、恢复或完成试验。
- 设备观察员：配置温控曲线，向运行中的试验提交传感帧。
- 质量研究员：查看阶段偏差、告警数量和最终报告。

## 核心实体
试样包含材料、安全温区和占用状态；温控曲线由多个升降温阶段组成；试验把一件试样与一条曲线绑定；传感帧带有严格递增序列；报告聚合每个阶段的最小值、最大值、平均值与告警数。

## 业务流程
1. 登记试样：校验标签、材料和安全温区后保存 ready 试样。
2. 建立曲线：校验阶段名称、目标温度、斜坡时长、保持时长和容差。
3. 启动试验：核对曲线温度范围，原子化占用试样并创建 running 试验。
4. 采集传感帧：校验序列和采集时间，评估当前阶段容差，更新游标和计数。
5. 完成与报告：释放试样，聚合传感帧，保存可查询报告。

## 状态与规则
试样在 ready、in_use、retired 间流转；试验在 running、paused、completed、aborted 间流转。只有 running 试验接收传感帧；同一传感器序列必须递增；曲线目标必须落在试样安全窗口内；没有传感证据的试验不能完成。

## 接口与验证
服务默认监听 `:8096`，提供 `/health`、`/api/specimens`、`/api/profiles`、`/api/runs`、传感帧、暂停恢复、完成、事件和报告接口。运行清单会启动独立端口，依次验证健康检查以及五条完整 workflow。
